package com.learningportal.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for security headers and HTTPS settings
 * Note: Security headers are now configured in the main SecurityConfig
 */
@Configuration
public class SecurityHeadersConfig {
    // Security headers configuration moved to SecurityConfig for simplicity
}